ruleset lab2 {
    meta {
        name "lab2"
        author "Jared"
        logging off
    }
    dispatch {
        // domain "exampley.com"
    }
    
    rule clear_Names {
        select when pageview '.*'
        notify("Working", "Working")
    }
}
